module.exports = {
    /*
     * Note: In a real production level project, this would be an environment variable
     * doing it this way only for simplicity and time's sake to have a
     * "proof of concept" before proejct presentations
     */
    secret: "super-duper secret secret"
}